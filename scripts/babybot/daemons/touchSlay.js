//
// this script WON'T work. for some strange reason, "on" works fine,
// but "slay" says @RPC server unavailable. in order to kill touch.exe
// get a console on oceanus and manually kill.
//

// var WshShell = WScript.CreateObject("WScript.Shell");

// WshShell.Run ("slay -n oceanus touch.exe", 7, true);
