headers:
-------------
1. add a #define msg type to meid-msg.h
2. add a msg structure to meid-msg.h
3. add the new msg structure to the union in meid-msg.h
4. add the client function prototype to meid.h

client side:
-------------
1. add a wrapper function to wrappers.c

daemon side:
-------------
1. add a daemon prototype to extern.h
2. add a case to the main.c loop
3. add the do_ function to either util.c or wraps.c
